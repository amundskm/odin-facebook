<li>
    <span><%= link_to comment.user.name, comment.user %></span>
    <span><%= comment.content %></span>
    <span><%= link_to 'Edit', '#'%></span>
    <%= link_to 'New Comment', new_comment_path( commentable_id: comment.id, commentable_type: "comment" ) %>
    <%= render comment.comments %>
</li>